# BioLens-Skill / spatial_single-cell — Python dependencies (Python >= 3.10)
# Install: pip install -r requirements.txt

# HTTP (geo-sra)
requests>=2.28.0

# Tables & numerics
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0

# Plotting
matplotlib>=3.5
seaborn>=0.12

# scRNA / spatial baseline (Scanpy stack)
scanpy>=1.9
anndata>=0.8
igraph>=0.10
leidenalg>=0.9

# Cell type annotation (celltypist-annotate)
celltypist>=1.6.0

# Optional (uncomment if needed):
# scvi-tools          # scvi-integrate deep batch correction
# squidpy             # spatial-qc / spatial-svg preferred path
# harmonypy           # spatial-integrate preferred batch correction
# spatialdata         # SpatialData-oriented workflows
