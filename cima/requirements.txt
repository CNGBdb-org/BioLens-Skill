# BioLens-Skill / cima — Python dependencies (Python >= 3.10)
# Install: pip install -r requirements.txt

# HTTP / catalog / FTP helpers (resource, atlas, GRN, xQTL, SMR)
requests>=2.28.0
certifi>=2023.0.0

# Tables & numerics
numpy>=1.21
pandas>=1.3
openpyxl>=3.0.0

# Plotting (atlas / CLM / pipeline figures)
matplotlib>=3.5
seaborn>=0.12

# scRNA / scATAC pipeline skills
scanpy>=1.9
anndata>=0.8
scipy>=1.7
scikit-learn>=1.0
igraph>=0.10
leidenalg>=0.9

# pseudobulk variance partition
statsmodels>=0.13

# Optional (uncomment if needed):
# harmonypy>=0.0.9          # or: harmony-pytorch — batch correction
# cosg>=0.1.0               # cima-cell-annotation COSG tables
# logomaker>=0.8            # cima-clm sequence logos
